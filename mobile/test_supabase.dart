import 'package:supabase/supabase.dart';

void main() async {
  final supabase = SupabaseClient('https://<YOUR_PROJECT_REF>.supabase.co', '<YOUR_ANON_KEY>');
  // Not sure of keys. Let's find them from lib/main.dart or .env
}
