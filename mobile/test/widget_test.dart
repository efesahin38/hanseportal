import 'package:flutter_test/flutter_test.dart';
import 'package:mobile/main.dart';

void main() {
  testWidgets('App starts with login screen', (WidgetTester tester) async {
    // Note: requires Supabase initialization – run with real credentials only
    // await tester.pumpWidget(const HanseApp());
    // expect(find.text('Hanse Kollektiv'), findsOneWidget);
  });
}
